module clock2 (out_clock,clk);

output reg out_clock = 1'b0;
input clk; 
integer count = 1;


always @(posedge clk)
begin 
	 if(count == 2) begin 
        count   <= 1;
        out_clock <= ~out_clock;
    end else begin
        count   <= count + 1;
    end 
end

endmodule